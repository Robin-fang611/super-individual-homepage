# 星路世界质感增强计划

## 现状诊断

当前问题不在于功能缺失，而在于**视觉层次扁平、色彩贫瘠、星光缺乏灵魂**。参考了 Genshin Impact、三体动画、新海诚星空、国风水墨场景等风格后，以下是可落地、不加依赖的增强方案。

---

## 一、核心矛盾

Three.js 内置的 UnrealBloomPass 需要额外导入，现有 `/assets/three.module.js` 不含 post-processing。但不能加依赖，所以**所有增强必须通过 Canvas 纹理、材质参数、场景层次来实现**。

---

## 二、分层增强方案

### Level 1：背景天穹 — 让深空"有颜色"

**当前问题**：背景几乎是纯黑到深蓝的双色渐变，像一张平铺的布。

**参考方向**：新海诚的夜空从来不是纯黑——深蓝里有紫、靛、暗金的气韵变化。

**改动**：
- 背景纹理增加 **3-4 层径向渐变**，从不同中心点出发（不只是线性）
- 引入极微弱的 **紫灰 (#1a1c2e)** 和 **靛蓝 (#0d1b2a)** 色区
- 在画面右上区域加极淡的暖色气韵 (warm tint 星云 patch)
- 暗角保留，但暗角边缘带一点冷蓝而不是纯黑

**效果**：深空"活"起来，有了气和色彩呼吸。

### Level 2：星河粒子 — 尺寸×2、透明度分层、色彩变化

**当前问题**：星河粒子单个只有 0.035 大小，全用 AdditiveBlending 混在一起，看起来像一层均匀的雾。

**参考方向**：Genshin 的星空里星星是**可辨识的亮点**而不是雾。

**改动**：
- 粒子系统分为 2-3 层：
  - 前景层：80 颗大粒子 (size 0.06-0.10)，高透明度，慢速流动 → **真正的"明星"**
  - 中景层：300 颗中等粒子 (size 0.03-0.05)，中速 → **星河的密度**
  - 背景层：400 颗小粒子 (size 0.015-0.025)，快速循环 → **星尘氛围**
- 大粒子中混入 10-15% 暖金色 (#d8b86a)，其余冷白
- 大粒子加微弱 pulsating glow halo（独立 Sprite）

**效果**：星夜有了**景深**和**可辨识的星星**，不再是均匀的雾。

### Level 3：星星节点 — 从"发光的点"变成"星芒"

**当前问题**：节点核心是 sphere mesh (或 sprite)，光晕是一个圆形渐变纹理。看起来像"电灯泡"不是"星星"。

**参考方向**：真实天文摄影中亮星有**衍射星芒**（4 条或 6 条射线）。

**改动**：
- 给当前节点或高亮节点加一个**星芒纹理**（用 Canvas 画交叉射线）
- 星芒作为独立 Sprite 挂在节点下方，仅在 hover/active 时显现
- 星芒透明度随星体呼吸一起律动

**效果**：重要节点一秒从"光点"升级为"恒星"。

### Level 4：雾层 — 从透明 Sprite 到有墨色层次

**当前问题**：水墨雾是均匀的半透明 Sprite，像一层均匀的薄纱。

**参考方向**：国风水墨的雾气有**浓淡干湿**变化，不是均匀的。

**改动**：
- ink mist 纹理不再用均匀的 radial gradient blob
- 改为**多层叠加工艺**：每层有不同的大小、透明度、色温
- 增加 2-3 个"墨核"（更浓的核心区域），让雾气有聚散感
- 雾气漂移动画增加 Z 轴浮动（不只是 XY）

**效果**：雾气从"薄纱"变成"水墨氤氲"。

### Level 5：色彩定调 — 全局色温统一

**当前问题**：#82dfff 冰蓝为主色，但没有环境色互相映衬。

**参考方向**：好的视觉设计有一个**主色+辅色+点缀色**的结构。

**建议色板**：
| 角色 | 色值 | 用途 |
|------|------|------|
| 主色 (深空) | #02040b → #0d1b2a | 背景基调 |
| 辅色 (星云) | #1a1c2e, #1b2d45 | 中景雾层 |
| 主光 (星光) | #d8e6f0 → #b9f1ff | 星星主体 |
| 点缀色 | #82dfff (冰蓝), #d8b86a (暖金) | 高光/节点 |
| 极暗部 | #01030a | 左侧暗场 |

---

## 三、实施顺序

1. **粒子分层** (最大视觉收益/最小改动) — 把现在的单层粒子拆成 3 层
2. **背景纹理丰富** — 增加多中心径向渐变 + 暖色气韵
3. **星芒纹理** — 为重要节点绘制星芒 Sprite
4. **雾层重做** — 改为有多核浓度的水墨雾
5. **色彩微调** — 全局颜色统一

---

## 四、不需做的

- ❌ 不引入 post-processing (无法加依赖)
- ❌ 不重写渲染管线
- ❌ 不加新几何体
- ❌ 不改 Three.js 版本
